rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Toda escritura y lectura pasa por FastAPI con Firebase Admin.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}